# πOS 開發手冊

> 主要維護目標是 `/main/`。`/demo/` 只作預覽快照，不主動修改。

## App 開發模式

πOS app 支援兩種 UI 開發模式。兩者可以混用，但建議新 app 優先採用 JSON UI，只有需要高度自訂互動時才手寫 HTML。

### 模式 A：JSON UI Schema

適合設定頁、表單、工具面板、資料列表、簡單管理介面。

優點：
- UI 寫法統一。
- 可由 `PiOS.ui` 生成一致風格的控制項。
- 更容易自動套用多語言、設定持久化、權限檢查與無障礙屬性。
- 未來可做視覺化 app builder。

建議格式：

```json
{
  "type": "panel",
  "title": "app:settings.display.title",
  "children": [
    {
      "type": "slider",
      "label": "app:settings.display.scale",
      "min": 80,
      "max": 130,
      "setting": "ui_scale",
      "onChange": "settings.applyScale"
    },
    {
      "type": "select",
      "label": "app:settings.display.taskbar_pos",
      "setting": "taskbar_pos",
      "options": [
        { "label": "app:settings.display.taskbar_bottom", "value": "bottom" },
        { "label": "app:settings.display.taskbar_top", "value": "top" }
      ]
    }
  ]
}
```

目前狀態：JSON UI renderer 是規劃中的核心 API。現階段 app 可先保留 schema 結構，或用手寫 HTML 實作同等 UI。

### 模式 B：自寫 HTML/JS

適合檔案總管、終端機、瀏覽器、VM、繪圖工具、遊戲等高互動 app。

規則：
- app 可以在 `render(body, args, winId)` 裡自行建立 HTML。
- 必須優先使用 `PiOS.*` 統一 API，不要直接耦合底層模組。
- 只有核心 app 需要時才直接碰 `VFS`、`WM`、`OS`，並應逐步改回 `PiOS`。
- 所有可見功能都必須有真實行為，不能只放空按鈕。

## 統一 API

`PiOS` 是 app 呼叫系統能力的唯一推薦入口。

### 檔案 API

```js
await PiOS.fs.list('/Documents');
await PiOS.fs.readText('/Documents/a.txt');
await PiOS.fs.writeText('/Documents/a.txt', 'hello');
await PiOS.fs.copy('/Documents/a.txt', '/Desktop/a.txt');
await PiOS.fs.rename('/Desktop/a.txt', '/Desktop/b.txt');
await PiOS.fs.remove('/Desktop/b.txt');
```

### App 與視窗 API

```js
PiOS.app.launch('notepad', { path: '/Documents/a.txt' });
PiOS.window.close(winId);
PiOS.window.minimize(winId);
PiOS.window.list();
```

### 程序 API

```js
const processes = PiOS.process.list();
PiOS.process.kill(101);
```

### 剪貼簿 API

```js
await PiOS.clipboard.write('text');
const value = await PiOS.clipboard.read();
```

### 設定 API

```js
PiOS.settings.set('taskbar_pos', 'top');
const value = PiOS.settings.get('taskbar_pos', 'bottom');
```

### UI API

```js
PiOS.ui.notify('完成', '檔案已儲存');
PiOS.ui.contextMenu(x, y, [
  { label: '開啟', action: () => {} },
  { sep: true },
  { label: '刪除', action: () => {} }
]);
```

### 指令 API

所有終端機指令都應登錄到 `PiOS.command`，不要只寫在 Terminal app 裡。

```js
PiOS.command.register('hello', async args => {
  return 'Hello ' + (args[0] || 'πOS');
}, { description: '測試指令' });

const result = await PiOS.command.exec('hello world', { cwd: '/' });
```

## App Bundle 規格

每個 app 位於 `/main/apps/{id}/`。

```text
{id}/
  info.json
  app.js
  icon.svg
  en.json
  zh-tw.json
  zh-cn.json
```

`info.json`：

```json
{
  "id": "appid",
  "version": "1.0.0",
  "minOsVer": "1.0.0",
  "entry": "app.js",
  "icon": "icon.svg",
  "uiMode": "json | html | json-preferred",
  "installable": true,
  "launch": {
    "standalone": true,
    "shortcut": true,
    "url": "./?app=appid"
  },
  "rewriteTarget": "app-v2",
  "permissions": ["fs.read", "fs.write"],
  "background": false,
  "autostart": false,
  "category": "system"
}
```

## PWA 與加入主畫面

πOS 本體是主要 PWA。每個 app 透過 `info.json` 的 `launch.url` 暴露捷徑，manifest 可將常用 app 加入 shortcuts。

規則：
- `main/manifest.webmanifest` 描述 OS 本體與常用 app shortcuts。
- `main/sw.js` 提供離線快取。
- `PiOS.pwa.install()` 觸發瀏覽器安裝提示。
- `?app=notepad` 會在 OS 開機後直接啟動指定 app。
- app 不應自行註冊 service worker；統一由 OS 管理。

## App 最低品質標準

- 按鈕、選單、切換器、輸入框必須有真實行為。
- 會改變系統狀態的功能必須持久化或明確告知只在本次工作階段有效。
- 檔案操作必須透過 `PiOS.fs`。
- 跨 app 資料交換必須透過 `PiOS.clipboard` 或 VFS。
- 使用者可見錯誤必須用通知、狀態列或對話框回報。
- 所有 app 必須能被 `PiOS.app.launch(id, args)` 啟動。
- 新增系統能力時，先擴充 `PiOS` API，再讓 app 呼叫它。

## 目前優先工作

1. 建立 App Store / 第三方 app 安裝規格。
2. 建立權限授權 UI，而不是只在 console warning。
3. 將 Explorer 剩餘直接 VFS 呼叫逐步收斂到 `PiOS.fs`。
4. 將 Browser 的 `pios://` 本機頁擴充為可掛載的 system pages。
5. 將 VM 的 v86 映像檔下載狀態與 serial output 接成正式面板。

## 內建 App v2 標準

目前內建 app 已進入 v2：

- Explorer v2：真 VFS 檔案管理。
- Notepad v2：真 VFS 編輯器。
- Terminal v2：統一命令執行器。
- Browser v2：本機首頁、多分頁與安全限制回報。
- VM v2：真 v86 啟動器與環境診斷。
- Settings v2：JSON UI schema 驅動。

v2 app 不允許保留無作用按鈕；瀏覽器限制或未完成硬體能力必須在 UI 中明確說明。
